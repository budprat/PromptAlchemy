# PromptAlchemy Environment Variables
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to git!

# ============================================
# Required Environment Variables
# ============================================

# PostgreSQL Database Connection (Neon)
# Get your connection string from: https://neon.tech
# Format: postgresql://username:password@host/database?sslmode=require
DATABASE_URL=postgresql://your_user:your_password@your_host.neon.tech/your_database

# ============================================
# Optional Environment Variables
# ============================================

# OpenAI API Key for prompt optimization and evaluation
# Get your API key from: https://platform.openai.com/api-keys
# If not set, the app will use mock data in development mode
OPENAI_API_KEY=sk-your_openai_api_key_here

# Environment Mode (development | production)
# Default: development
NODE_ENV=development

# Replit Platform ID (automatically set on Replit)
# Only needed when running on Replit platform
# REPL_ID=your_repl_id

# ============================================
# Development Notes
# ============================================
#
# 1. DATABASE_URL is REQUIRED - the application will fail to start without it
# 2. OPENAI_API_KEY is optional:
#    - If not set: Mock data is returned for optimization and evaluation
#    - If set to "mock-key-for-development": Mock data is returned
#    - If set to real key: Real OpenAI API calls are made (costs apply)
# 3. NODE_ENV controls:
#    - development: Vite dev server with hot reload
#    - production: Serve static built files
#
